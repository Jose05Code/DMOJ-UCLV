#include <bits/stdc++.h>
    using namespace std;
    int main(){
        int x; cin >> x;
        while (x--)
        {
           unsigned long long n, o; cin >> n;
           cout << (long long int)pow(n, 1/2.) << "\n";
        }
    }